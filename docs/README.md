# test-suit — docs

Per-component and deeper documentation for the Nolus UAT integration tests. The project
root `CLAUDE.md` is the entry point; this directory holds detail that doesn't belong
inline there.

## Index

- **Architecture** — test-suite layout, env-prep flow, how suites reach the chain; placeholder
- **Runbooks** — env setup, funding, running a single suite, debugging flaky txs; placeholder
- **Decisions** — design decisions and their rationale; placeholder
